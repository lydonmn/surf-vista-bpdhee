// useSubscriptionGuard has been intentionally disabled.
// The paywall should only appear when a user tries to access a paywalled feature (e.g. videos),
// not as a global redirect. Use router.push("/paywall") at the feature level instead.
export function useSubscriptionGuard() {}
